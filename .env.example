# Strategy Backtesting Engine — environment variables
# Copy this file to .env and fill in any values before running.
# Currently no secrets are required (yfinance pulls public data).
# Reserved for future use (e.g. hosted deployment config).

# FLASK_ENV=development
# FLASK_PORT=5000
